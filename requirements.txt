# Core (pinned to versions known to work in development)
langchain==1.2.0
langchain-core==1.2.3
langchain-community==0.4.1
qdrant-client==1.16.2

# LangChain subpackages / integrations (loose pins — update if needed)
langchain-text-splitters>=0.1.0
langchain-groq>=0.0.1
langchain-google-genai>=0.0.1  # Optional: only needed if using Gemini embeddings
langchain-huggingface>=0.0.1  # For Sentence Transformers local embeddings
langgraph>=0.0.1

# Utilities
python-dotenv>=0.21.0
pydantic>=2.0.0
pymupdf>=1.22.0
Pillow>=9.0.0  # For image processing in PDFs
pandas>=1.5.0  # For data manipulation

# Optional extras
# BM25 retriever (optional)
rank_bm25>=0.2.2
# If you prefer the standalone LangChain -> Qdrant integration
# langchain-qdrant>=0.0.1
langchain-ollama>=1.0.0
langchain-qdrant>=1.1.0
streamlit>=1.30.0
watchdog>=3.0.0
transformers>=4.36.0
sentence-transformers>=2.2.2
gtts>=2.5.0  # For text-to-speech pronunciation functionality
